#ifndef GLOBAL_H
#define GLOBAL_H

//#define USE_RPI_GPIO_PARALLEL // Define to use RPi GPIO (only > rpi 2b)
//#define IOWAIT 0xFFFF // Time to wait between serial transmits. (number of NOPs to do, for no IOWAIT, do not define this)

#endif
